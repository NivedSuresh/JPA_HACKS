# Open transaction (via entity manager) in View/Controller
# Closes the EntityManager once the transaction ends, improving resource management
# and reducing potential lazy loading issues in the view.
spring.jpa.open-in-view=false

# Disables automatic committing of each SQL operation, allowing
# manual control over transactions for better consistency and performance.
spring.datasource.hikari.auto-commit=false

